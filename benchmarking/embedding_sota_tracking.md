# Embedding SOTA Tracking

This file tracks incremental changes and experiments for the embedding generation SOTA investigation. Treat prior notes as directional context; final ranking requires fresh, corrected benchmark evidence.

| i | commit | what tried | what failed / succeeded / learnt | next experiment motivation |
|---:|---|---|---|---|
| 1 | `ccd6bac9` | Audited and corrected the current embedding benchmark script before running new benchmarks. Added endpoint response validation, shared `--max-chars` support for in-process vLLM, matching local/nightly input slice and text cap, Dynamo embedding pooling patch wiring, and focused Docker-only tests. | Succeeded: Docker tests passed (`7 passed`) and compile checks passed. Learnt: the script previously could silently accept endpoint responses with missing/duplicate indexes, corrupting row count and throughput; endpoint-only `--endpoint-max-chars` was not enough for fair in-process comparison. Commit used `--no-verify` because the benchmark image lacks `git-lfs`, which breaks repo hooks inside Docker. | Run corrected benchmarks to establish a trustworthy in-process baseline, then test endpoint improvements that preserve correctness: pretokenized endpoint inputs, base64 responses, larger request batches, and Ray Serve handle/direct path if implemented. |
| 2 | `a2418640` | Single-YAML corrected baseline with `max_chars`/`endpoint_max_chars` 1500. Entries used stable session `embedding-sota-investigation` and suffixed names `*_c7264f1c_i2`. | Directional only, not final ranking evidence: Ray Data in-process succeeded with 1,023,449 docs, 262 input files, and 3571.74 docs/s; Xenna in-process succeeded with 1,023,449 docs, 262 input files, and 4150.21 docs/s. The run was stopped before endpoint entries because char caps are not representative of real embedding workloads. Previous agent's uncommitted diffs were preserved in `stash@{0}` before this run. | Rerun the baseline without any character cap. Keep in-process entries on `vllm_text_pretokenized`; raw `vllm_text` is now guarded behind an explicit opt-in flag for intentional regression experiments. |
| 3 | `f5eaed3c` | Uncapped corrected baseline in the single working YAML. Entries are renamed to `*_f5eaed3c_i3`, in-process entries use `vllm_text_pretokenized`, and no `--max-chars`, `--endpoint-max-chars`, or artificial endpoint truncate-token flags are set. | Mixed: Ray Data in-process pretokenized succeeded with 1,023,449 docs, 262 input files, no char caps, and 2150.33 docs/s. Xenna in-process pretokenized succeeded with 1,023,449 docs, 262 input files, no char caps, and 2394.53 docs/s. Ray Serve endpoint failed because vLLM rejected prompts over the 2048-token model context when `truncate_prompt_tokens` was omitted. Dynamo endpoint failed with `Failed to fold embeddings stream`, consistent with endpoint text mode not safely handling over-context uncapped prompts. | Keep the in-process results; rerun endpoint entries with model-context token truncation as the default endpoint behavior, still with no character cap. |
| 4 | pending | Endpoint-only uncapped rerun with entries `*_48c6b49f_i4` after changing endpoint default truncation semantics: no character cap, but default `endpoint_truncate_prompt_tokens <= 0` now resolves to the model context length. | Pending. This should match in-process pretokenized behavior, which tokenizes with `truncation=True` and `max_length=model_config.max_model_len`. | Determine whether endpoint paths are merely slower than in-process or were previously failing because endpoint text mode omitted model-context truncation. |
