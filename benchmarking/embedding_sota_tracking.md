# Embedding SOTA Tracking

This file tracks incremental changes and experiments for the embedding generation SOTA investigation. Treat prior notes as directional context; final ranking requires fresh, corrected benchmark evidence.

| i | commit | what tried | what failed / succeeded / learnt | next experiment motivation |
|---:|---|---|---|---|
| 1 | `ccd6bac9` | Audited and corrected the current embedding benchmark script before running new benchmarks. Added endpoint response validation, shared `--max-chars` support for in-process vLLM, matching local/nightly input slice and text cap, Dynamo embedding pooling patch wiring, and focused Docker-only tests. | Succeeded: Docker tests passed (`7 passed`) and compile checks passed. Learnt: the script previously could silently accept endpoint responses with missing/duplicate indexes, corrupting row count and throughput; endpoint-only `--endpoint-max-chars` was not enough for fair in-process comparison. Commit used `--no-verify` because the benchmark image lacks `git-lfs`, which breaks repo hooks inside Docker. | Run corrected benchmarks to establish a trustworthy in-process baseline, then test endpoint improvements that preserve correctness: pretokenized endpoint inputs, base64 responses, larger request batches, and Ray Serve handle/direct path if implemented. |
| 2 | `a2418640` | Single-YAML corrected baseline with `max_chars`/`endpoint_max_chars` 1500. Entries used stable session `embedding-sota-investigation` and suffixed names `*_c7264f1c_i2`. | Directional only, not final ranking evidence: Ray Data in-process succeeded with 1,023,449 docs, 262 input files, and 3571.74 docs/s; Xenna in-process succeeded with 1,023,449 docs, 262 input files, and 4150.21 docs/s. The run was stopped before endpoint entries because char caps are not representative of real embedding workloads. Previous agent's uncommitted diffs were preserved in `stash@{0}` before this run. | Rerun the baseline without any character cap. Keep in-process entries on `vllm_text_pretokenized`; raw `vllm_text` is now guarded behind an explicit opt-in flag for intentional regression experiments. |
| 3 | `f5eaed3c` | Uncapped corrected baseline in the single working YAML. Entries are renamed to `*_f5eaed3c_i3`, in-process entries use `vllm_text_pretokenized`, and no `--max-chars`, `--endpoint-max-chars`, or artificial endpoint truncate-token flags are set. | Mixed: Ray Data in-process pretokenized succeeded with 1,023,449 docs, 262 input files, no char caps, and 2150.33 docs/s. Xenna in-process pretokenized succeeded with 1,023,449 docs, 262 input files, no char caps, and 2394.53 docs/s. Ray Serve endpoint failed because vLLM rejected prompts over the 2048-token model context when `truncate_prompt_tokens` was omitted. Dynamo endpoint failed with `Failed to fold embeddings stream`, consistent with endpoint text mode not safely handling over-context uncapped prompts. | Keep the in-process results; rerun endpoint entries with model-context token truncation as the default endpoint behavior, still with no character cap. |
| 4 | `1157f44f` | Endpoint-only uncapped rerun with entries `*_48c6b49f_i4` after changing endpoint default truncation semantics: no character cap, but default `endpoint_truncate_prompt_tokens <= 0` resolves to the model context length. | Failed. Ray Serve no longer hit the over-context validation error, but failed with HTTP `ReadError`/OpenAI `APIConnectionError` after reading all 262 files and starting 16 client tasks. Dynamo still failed with `Failed to fold embeddings stream`. Learnt: endpoint raw-text mode is not a correct/stable baseline on uncapped documents even with model-context token truncation. | Rerun endpoint entries with client-side pretokenized `token_ids`, no character cap, model-context token truncation, and base64 responses to match in-process tokenization semantics and reduce response serialization overhead. |
| 5 | `22efeee8` | Endpoint-only uncapped rerun with entries `*_918bd744_i5`: `endpoint_input_format=token_ids`, `endpoint_encoding_format=base64`, request batch size 8, 16 client tasks, 64 concurrent requests per client, 4 replicas, model-context token truncation, and no character cap. | Mixed. Ray Serve still failed with `httpcore.ReadError` / OpenAI `APIConnectionError` before producing throughput, even though metrics confirmed tokenized input/base64/no char caps. Dynamo succeeded on 1,023,449 docs and 262 input files with `pretokenized=true`, `endpoint_pretokenized=true`, `endpoint_truncate_prompt_tokens=2048`, no char caps, 504.61s end-to-end, 92.19s startup, and 2028.21 docs/s end-to-end. Excluding Dynamo service startup gives about 2481.61 docs/s steady-state pipeline rate, so startup-inclusive ranking and persistent-service ranking differ. | Keep in-process entries locked to `vllm_text_pretokenized`. Next isolate endpoint overhead: either lower Ray Serve client pressure to diagnose HTTP transport failure, or tune Dynamo request batch/concurrency because its steady-state rate can exceed the current in-process baselines even though its batch end-to-end throughput does not. |
| 6 | `b740450d` | Ray Serve-only uncapped rerun with entry `embedding_generation_ray_serve_endpoint_a0d40d09_i6`: kept `endpoint_input_format=token_ids`, `endpoint_encoding_format=base64`, request batch size 8, 16 client tasks, 4 replicas, model-context token truncation, and no character cap; reduced only per-client concurrent requests from 64 to 8, lowering aggregate max requests from 1024 to 128. | Succeeded. Processed 1,023,449 docs from 262 input files with `pretokenized=true`, `endpoint_pretokenized=true`, `endpoint_truncate_prompt_tokens=2048`, no char caps, 1120.43s end-to-end, 63.94s startup, and 913.45 docs/s end-to-end. Excluding service startup gives about 968.72 docs/s. Learnt: Ray Serve's i5 failure was load-sensitive HTTP/ingress pressure, not embedding semantics, but the stable lower-pressure point is far slower than in-process and Dynamo. | Sweep Ray Serve concurrency upward one variable at a time to find the fastest stable point; next try per-client concurrent requests 32 or 16 while keeping token_ids/base64/no char caps. |
| 7 | `485d2b83` | Ray Serve-only uncapped rerun with entry `embedding_generation_ray_serve_endpoint_3109d174_i7`: kept token_ids/base64, request batch size 8, 16 client tasks, 4 replicas, model-context token truncation, and no character cap; increased only per-client concurrent requests from 8 to 32, raising aggregate max requests from 128 to 512. | Succeeded but slower. Processed 1,023,449 docs from 262 input files with `pretokenized=true`, `endpoint_pretokenized=true`, `endpoint_truncate_prompt_tokens=2048`, no char caps, 1175.05s end-to-end, 63.31s startup, and 870.98 docs/s end-to-end. Excluding service startup gives about 920.58 docs/s. Learnt: higher Ray Serve HTTP concurrency did not improve throughput; it increased transport/client churn while remaining correct. | Test the midpoint, per-client concurrent requests 16 / aggregate 256, only if Ray Serve tuning still matters. Current Ray Serve best among tested is aggregate 128, but it is still far behind in-process and Dynamo. |
| 8 | `92b5266c` | Dynamo-only uncapped rerun with entry `embedding_generation_dynamo_endpoint_a0ce08c9_i8`: kept token_ids/base64, 16 client tasks, 64 concurrent requests per client, 4 replicas, model-context token truncation, and no character cap; increased only `endpoint_request_batch_size` from 8 to 16. | Succeeded. Processed 1,023,449 docs from 262 input files with `pretokenized=true`, `endpoint_pretokenized=true`, `endpoint_truncate_prompt_tokens=2048`, no char caps, 496.51s end-to-end, 89.64s startup, and 2061.27 docs/s end-to-end. Excluding service startup gives about 2515.39 docs/s. Learnt: batch size 16 modestly improves Dynamo over batch size 8, but not enough to beat Xenna in the startup-inclusive batch-job ranking. | Try Dynamo request batch size 32 to see whether larger request batches continue to reduce transport overhead or start hurting due to payload size/backpressure. |
| 9 | `05ab6af2` | Dynamo-only uncapped rerun with entry `embedding_generation_dynamo_endpoint_24aa9d10_i9`: kept token_ids/base64, 16 client tasks, 64 concurrent requests per client, 4 replicas, model-context token truncation, and no character cap; increased only `endpoint_request_batch_size` from 16 to 32. Docker validation also confirmed the local YAML uses `vllm_text_pretokenized` for in-process entries, token_ids for endpoint entries, and no character caps; a second Docker check confirmed the script still has the pretokenized default/guard and the vLLM stage token-ID path. | Succeeded but regressed slightly. Processed 1,023,449 docs from 262 input files with `pretokenized=true`, `endpoint_pretokenized=true`, `endpoint_truncate_prompt_tokens=2048`, no char caps, 501.20s end-to-end, 92.83s startup, and 2041.99 docs/s end-to-end. Excluding service startup gives about 2506.17 docs/s. Learnt: request batch size 32 is correct but a bit slower than batch size 16, likely because larger request payload/backpressure offsets fewer HTTP requests. | Keep Dynamo request batch size 16 as the best tested Dynamo point. Next tune in-process pretokenized vLLM batch size/worker geometry, starting with Xenna batch size 64, because current end-to-end winner is Xenna at batch size 32 and it has not been swept. |
| 10 | `10293a77` | Xenna-only uncapped rerun with entry `embedding_generation_xenna_64a9b7f7_i10`: kept in-process `vllm_text_pretokenized`, 4 model workers, the same 262-file dataset slice, and no character cap; increased only `model_inference_batch_size` from 32 to 64. | Succeeded but regressed. Processed 1,023,449 docs from 262 input files with `pretokenized=true`, no char caps, 437.51s end-to-end, and 2339.28 docs/s. Learnt: larger in-process stage batches do not improve Xenna here; batch size 64 likely adds padding/queueing or vLLM scheduling pressure without increasing useful GPU throughput. | Try Xenna batch size 16 to bracket the current best batch size 32 from below. If 16 is also slower, keep Xenna batch size 32 as the best tested in-process end-to-end point. |
| 11 | `89b1540f` | Prepared Xenna-only uncapped rerun with entry `embedding_generation_xenna_61c0107f_i11`: keep in-process `vllm_text_pretokenized`, 4 model workers, the same 262-file dataset slice, and no character cap; decrease only `model_inference_batch_size` from 32 to 16. | Pending run. | If batch size 16 improves, keep sweeping downward/upward around the best point; if it regresses, Xenna batch size 32 is bracketed as the best tested in-process end-to-end point. |
