paths:
  - name: "datasets_path"
    host_path: "/raid/praateekm/datasets"
    container_path: "/raid/praateekm/datasets"
  - name: "model_weights_path"
    host_path: "/raid/praateekm/hf_cache"
    container_path: "/raid/praateekm/hf_cache"
  - name: "results_path"
    host_path: "/raid/praateekm/curator-nightly/results"
    container_path: "/raid/praateekm/curator-nightly/results"

datasets:
  - name: "tinystories"
    formats:
      - type: "parquet"
        path: "/raid/praateekm/datasets/fineweb-edu-fortified-5m-1280"
  - name: "text_models_hf_cache"
    formats:
      - type: "files"
        path: "/raid/praateekm/hf_cache"

sinks:
  - name: "slack"
    enabled: false

entries:
  - name: embedding_generation_raydata_fracgpu_f7a9ad75_i16
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=vllm_text_pretokenized
      --model-num-workers=16
      --model-worker-gpus=0.249
      --model-gpu-memory-utilization=0.22
      --cache-dir={dataset:text_models_hf_cache,files}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 1200
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100

  - name: embedding_generation_xenna_fracgpu_9453a1a9_i17
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=vllm_text_pretokenized
      --model-num-workers=16
      --model-worker-gpus=0.249
      --model-gpu-memory-utilization=0.22
      --cache-dir={dataset:text_models_hf_cache,files}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 1200
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100

  - name: embedding_generation_ray_serve_endpoint_3109d174_i7
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=ray_serve_endpoint
      --cache-dir={dataset:text_models_hf_cache,files}
      --server-replicas=4
      --client-num-workers=16
      --endpoint-max-concurrent-requests=32
      --endpoint-input-format=token_ids
      --endpoint-request-batch-size=8
      --endpoint-encoding-format=base64
      --endpoint-client-mode=tasks
      --endpoint-request-timeout-s=900
      --endpoint-pooler-config={}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 2400
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100

  - name: embedding_generation_ray_serve_haproxy_http_ca31a35e_i18
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=ray_serve_endpoint
      --cache-dir={dataset:text_models_hf_cache,files}
      --server-replicas=4
      --client-num-workers=16
      --endpoint-max-concurrent-requests=64
      --endpoint-input-format=token_ids
      --endpoint-request-batch-size=8
      --endpoint-encoding-format=base64
      --endpoint-client-mode=tasks
      --endpoint-request-timeout-s=900
      --endpoint-pooler-config={}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 2400
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100

  - name: embedding_generation_ray_serve_haproxy_handle_ca31a35e_i19
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=ray_serve_endpoint
      --cache-dir={dataset:text_models_hf_cache,files}
      --server-replicas=4
      --client-num-workers=16
      --endpoint-max-concurrent-requests=64
      --endpoint-input-format=token_ids
      --endpoint-request-batch-size=8
      --endpoint-encoding-format=base64
      --endpoint-client-mode=ray_handle
      --endpoint-request-timeout-s=900
      --endpoint-pooler-config={}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 2400
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100

  - name: embedding_generation_ray_serve_haproxy_http_db6443b4_i20
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=ray_serve_endpoint
      --cache-dir={dataset:text_models_hf_cache,files}
      --server-replicas=4
      --client-num-workers=16
      --endpoint-max-concurrent-requests=64
      --endpoint-input-format=token_ids
      --endpoint-request-batch-size=8
      --endpoint-encoding-format=base64
      --endpoint-client-mode=tasks
      --endpoint-request-timeout-s=900
      --endpoint-pooler-config={}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 2400
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100

  - name: embedding_generation_ray_serve_haproxy_handle_db6443b4_i21
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=ray_serve_endpoint
      --cache-dir={dataset:text_models_hf_cache,files}
      --server-replicas=4
      --client-num-workers=16
      --endpoint-max-concurrent-requests=64
      --endpoint-input-format=token_ids
      --endpoint-request-batch-size=8
      --endpoint-encoding-format=base64
      --endpoint-client-mode=ray_handle
      --endpoint-request-timeout-s=900
      --endpoint-pooler-config={}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 2400
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100

  - name: embedding_generation_ray_serve_haproxy_handle_4fb16baf_i22
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=ray_serve_endpoint
      --cache-dir={dataset:text_models_hf_cache,files}
      --server-replicas=4
      --client-num-workers=16
      --endpoint-max-concurrent-requests=64
      --endpoint-input-format=token_ids
      --endpoint-request-batch-size=8
      --endpoint-encoding-format=base64
      --endpoint-client-mode=ray_handle
      --endpoint-request-timeout-s=900
      --endpoint-pooler-config={}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 2400
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100

  - name: embedding_generation_ray_serve_haproxy_http_d97e3165_i23
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=ray_serve_endpoint
      --cache-dir={dataset:text_models_hf_cache,files}
      --server-replicas=4
      --client-num-workers=16
      --endpoint-max-concurrent-requests=64
      --endpoint-input-format=token_ids
      --endpoint-request-batch-size=8
      --endpoint-encoding-format=base64
      --endpoint-client-mode=tasks
      --endpoint-request-timeout-s=900
      --endpoint-pooler-config={}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 2400
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100

  - name: embedding_generation_dynamo_endpoint_24aa9d10_i9
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:tinystories,parquet}
      --load-dataset-ratio=0.2
      --model-identifier=google/embeddinggemma-300m
      --model-variation=dynamo_endpoint
      --cache-dir={dataset:text_models_hf_cache,files}
      --server-replicas=4
      --client-num-workers=16
      --endpoint-max-concurrent-requests=64
      --endpoint-input-format=token_ids
      --endpoint-request-batch-size=32
      --endpoint-encoding-format=base64
      --endpoint-client-mode=tasks
      --endpoint-request-timeout-s=900
      --endpoint-pooler-config={}
    ray:
      num_cpus: 64
      num_gpus: 4
    timeout_s: 1800
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 100
